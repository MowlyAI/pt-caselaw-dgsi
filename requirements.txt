# ── API runtime (Docker image) ──────────────────────────────────────────────
# Pipeline-only deps (supabase client, scraper libs, etc.) are NOT listed here;
# install them separately for local pipeline work: pip install supabase aiohttp …

# CLI & progress
rich>=13.9.4
tqdm==4.66.4
click==8.1.7

# Data
ujson==5.10.0
python-dotenv>=1.1.0

# LLM & API
httpx>=0.28.1
openai==1.30.1
pydantic>=2.11.7

# Database
asyncpg==0.29.0

# FastAPI
fastapi>=0.111.0
uvicorn[standard]>=0.35

# MCP
fastmcp>=3.0

# Utils
tenacity==8.3.0
asyncio-throttle==1.0.2
